# Core dependencies
click>=8.0.0
rich>=13.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
PyYAML>=6.0
jinja2>=3.1.0
requests>=2.31.0

# Note: External security tools (nuclei, httpx, subfinder, etc.)
# are CLI tools and should be installed separately.
# See docs/installation.md for setup instructions.
